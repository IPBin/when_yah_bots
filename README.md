# when_yah_bots

## Usage

Run the pipeline on a single case:

```
python3 run.py --image image.nii.gz --aorta-mask aorta_mask.nii.gz --output prediction.json
```

Use `python3` explicitly (not `python`) on systems where `python` isn't aliased to Python 3.

