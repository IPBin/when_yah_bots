# Challenge brief

> Paste the exact Branchseed challenge brief text here (verbatim), so that every
> module and prompt in `branchseed_playbook.md` can reference `docs/brief.md`
> as the source of truth for exact requirements and output format.

This file is a placeholder — the actual brief text was not supplied alongside
`branchseed_playbook.md`. Replace this content with the real brief before
relying on any spec details that are only referenced (not quoted) in the
playbook.
