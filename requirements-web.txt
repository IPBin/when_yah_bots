# Optional local web console (webapp/). Not required for the scored CLI
# pipeline -- see requirements.txt for that. Install with:
#   pip install -r requirements.txt -r requirements-web.txt
Flask==3.1.3
